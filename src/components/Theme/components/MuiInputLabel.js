import { palette } from '../palette'

export const MuiInputLabel = {
    styleOverrides: {
        root: {
            color: palette.text.main,
            fontFamily: 'Roboto',
            fontWeight: 500,
        },
    },
}
